import { ConvertToNumberPipe } from './convert-to-number.pipe';

describe('ConvertToNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
